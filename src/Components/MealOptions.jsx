import React from "react";

const MealOptions =  ({ name }) => {
    return (
      <block className="min-h-screen flex items-center justify-center">
        {name}
      </block>
    );
  };

export default MealOptions;
